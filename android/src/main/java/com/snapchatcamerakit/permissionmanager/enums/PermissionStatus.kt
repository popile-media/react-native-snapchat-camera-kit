package com.snapchatcamerakit.permissionmanager.enums

class PermissionStatus {
  companion object {
    const val DENIED = "denied"
    const val AUTHORIZED = "authorized"
    const val NOT_DETERMINED = "not-determined"
  }
}
